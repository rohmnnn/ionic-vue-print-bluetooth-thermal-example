import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-vue-printing-bluetooth-serial-example',
  webDir: 'dist'
};

export default config;
